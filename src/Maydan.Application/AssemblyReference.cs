namespace Maydan.Application;

// Marker type so other layers can point reflection-based registration (e.g. AddValidatorsFromAssembly)
// at this assembly without hardcoding a concrete class name.
public sealed class AssemblyReference
{
}
